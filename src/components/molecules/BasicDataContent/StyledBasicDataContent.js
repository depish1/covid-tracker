import styled from 'styled-components';

const StyledBasicDataContent = styled.div`
  text-align: left;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 0.5rem;
`;

export default StyledBasicDataContent;
